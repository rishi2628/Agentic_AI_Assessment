[pytest]
# Register custom markers
markers =
    integration: marks tests that require Ollama to be running (deselect with -m "not integration")

# Run unit tests by default (no integration marker)
addopts = -v --tb=short

# Test discovery
testpaths = tests
